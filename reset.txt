1. reset --hard
2. reset --soft
3. reset --mixed

@~ - 1 коммит назад.
@~3 - 3 коммита назад.

          working           index
          directory

--hard:
1. передвигает HEAD (состояние репозитория) на коммит назад
2. очищает индекс:
3. удаляет все незакомиченные изменения
Если нужно вернуться к предыдущему состоянию HEAD: git reflog master / git reset --hard ORIG_HEAD
reflog хранит состояния в течении 30 дней, которые доступны.

